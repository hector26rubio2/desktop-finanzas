# Node
node_modules/
node_modules_old_del/
dist/
release/
.angular/
.opencode/
# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# OpenCode config (puede contener API keys)
opencode.json

# Environment files
.env
.env.local
.env.*.local
.env.production
.env.development

# Logs
*.log
npm-debug.log*
.agents

# Artefactos generados (regenerables, no se versionan)
graphify-out/
.shots/
# Google OAuth secrets
client_secret_*.json
# Lleva apiUrl, googleClientId y encryptionKey con valores reales. Nadie lo lee
# (provideAppConfig inyecta `environment`), pero angular.json copia src/assets/
# entera a dist/, así que se empaquetaba en el instalador.
src/assets/app-config.json
skills-lock.json
